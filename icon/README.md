# 通知图标配置

`NotifyIconsSupportConfig.json` 来自 [AndroidNotifyIconAdapt](https://github.com/fankes/AndroidNotifyIconAdapt) 的 `APP/NotifyIconsSupportConfig.json`，用于 MiPushFramework 远端配置同步后的默认通知小图标适配。

该上游项目以 GNU Affero General Public License v3.0 发布。随同分发的许可证文本见 `LICENSE-AndroidNotifyIconAdapt`。
